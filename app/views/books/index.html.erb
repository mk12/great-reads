<div class="single-page">
	<h1>Library</h1>

	<ul class="book-list">
		<% @books.each do |book| %>
			<%= link_to cover_info(book), book_path(book), class: 'book-item' %>
		<% end %>
		<%= link_to "+", '#', class: 'book-item add-book' %>
	</ul>
</div>
